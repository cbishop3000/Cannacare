import React from 'react'
import LoadingBar from './loadingbar.gif'

const Loading = () => {
  return (
    <div>
        <img src={LoadingBar} alt="my-gif" />
    </div>
  )
}

export default Loading